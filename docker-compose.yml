version: '3.9'

services:
  sgd_db:
    image: postgres
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: andres_dev
      POSTGRES_PASSWORD: development
      POSTGRES_DB: postgres
